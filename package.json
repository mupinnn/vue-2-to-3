{
  "name": "vue-2-to-3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -k \"pnpm --filter shared-lib dev\" \"wait-on ./packages/shared-lib/dist/Vue2To3SharedLib.es.js && turbo run dev --filter='./apps/*'\"",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.0.1",
    "turbo": "^1.8.8",
    "wait-on": "^7.0.1"
  }
}
